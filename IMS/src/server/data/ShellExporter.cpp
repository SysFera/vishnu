#include "ShellExporter.hpp"

int
ShellExporter::exporte(string oldSession, string filename, IMS_Data::ExportOp op){
  // Verifier que la session est close

  // Faire comme dans list history command mais sans decoder

  // Decoder chaque objet en format shell

  return 0;
}
