CC	= g++
LD	= g++
CFLAGS	= -Wall -g
LDFLAGS = -Wall -g
OBJ	= main.o Mapper_impl.o Mapper.o
INC	= -I/home/keo/Bureau/depot_git_edf/vishnu/core/exception

all	: main

main	: $(OBJ)
		$(LD) $(LDFLAGS) -o $@ $(OBJ)

main.o	: main.cc
		$(CC) $(CCFLAGS) -c $< -o $@ $(INC)

Mapper_impl.o	: Mapper_impl.cc
		$(CC) $(CCFLAGS) -c $< -o $@ $(INC)

Mapper.o	: Mapper.cc
		$(CC) $(CCFLAGS) -c $< -o $@ $(INC)
