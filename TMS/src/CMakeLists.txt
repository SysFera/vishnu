if (NOT CLIENT_ONLY)
  add_subdirectory(sed)
  add_subdirectory(server)
  add_subdirectory(utils_torque)
  add_subdirectory(slave)
endif (NOT CLIENT_ONLY)

add_subdirectory(api)
add_subdirectory(client)
add_subdirectory(cli)
